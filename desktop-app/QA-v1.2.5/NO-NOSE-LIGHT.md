# v1.2.5 大哭动画鼻子无亮光验收

- 验收对象：`/Applications/咕咕龙.app`
- 版本：`1.2.5`（build `125`）
- 尺寸：50%
- 动作：右键菜单 → 大哭
- 采样：连续 40 张运行画面，覆盖多个完整循环
- 结果：仅检测到精灵图原有的 8 个大哭帧，随后循环逐帧完全重复；不存在额外的鼻尖闪光帧、亮度变化帧或发光叠加层。

源码中的 `shouldFlashNose`、循环计数、渐变光晕和鼻尖高光绘制代码均已删除。

`cry-no-nose-light.png` 为本机安装版运行截图。
