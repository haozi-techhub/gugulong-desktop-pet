# 咕咕龙桌宠 v1.3.2 菜单栏 Logo 验收

- 使用 `MenuBarIconTemplate.png` 作为逻辑 1x 模板资源，由 AppKit 自动选择 `@2x` 表示。
- 菜单栏图标按 18×18 pt 渲染，状态项保持系统标准 24×24 pt 点击区域。
- 模板图启用 `template`，可随 macOS 深色/浅色菜单栏自动反色。
- 资源加载失败时使用系统符号兜底，避免顶部栏入口消失。
- 本机安装版本：1.3.2（142）。
- 本机辅助功能检查：已创建名为“咕咕龙”的菜单栏项目，尺寸为 24×24 pt。
- 本机截图检查：剪影清晰显示，桌宠与菜单栏入口同时存在。
